---
title: Hover Image Links
description: Hover Image Links are interactive links that change or reveal an image when hovered over, enhancing user engagement.
author: YourTwitterUsername
---

<ComponentPreview name="links-hover-image-links--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

```bash
npm install framer-motion lucide-react
```

<Step>Update `tailwind.config.js`</Step>

Add the following to your tailwind.config.js file.

```json
module.exports = {
  theme: {
    extend: {
    }
  }
}
```

<Step>Run the following command</Step>

It will create a new file `hover-image-links.tsx` inside the `components/mage-ui/links` directory.

```bash 
mkdir -p components/mage-ui/links && touch components/mage-ui/links/hover-image-links.tsx
```

<Step>Paste the code</Step> 

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/links/hover-image-links.tsx

```
</Steps>


