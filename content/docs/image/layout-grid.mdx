---
title: Layout Grid
description: A layout effect that animates the grid item on click, powered by framer motion layout
author: YourTwitterUsername
---

<ComponentPreview name="image-layout-grid--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

```bash
npm install framer-motion lucide-react
```

<Step>Update `tailwind.config.js`</Step>

Add the following to your tailwind.config.js file.

```json
module.exports = {
  theme: {
    extend: {
    }
  }
}
```

<Step>Run the following command</Step>

It will create a new file `layout-grid.tsx` inside the `components/mage-ui/image` directory.

```bash 
mkdir -p components/mage-ui/image && touch components/mage-ui/image/layout-grid.tsx
```

<Step>Paste the code</Step> 

Open the newly created file and paste the following code:

```jsx file=<rootDir>/mage-ui/image/layout-grid.tsx

```
</Steps>


